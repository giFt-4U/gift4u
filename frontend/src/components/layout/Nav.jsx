import React from 'react';
import { Link } from 'react-router-dom';
import { NavWrapper } from '../../styles/AppLayout';

const Nav = () => {
    return (
        <NavWrapper>
            <h1 Link to="/">따숨품</h1>
        </NavWrapper>
    );
};

export default Nav;